# Example of Streamlit secrets (never commit the real file).
# When deploying on Streamlit Community Cloud, paste these in the "Secrets"
# UI of the app settings. Local dev can ignore this file.

APP_PASSCODE = "12345"

# Needed by src/sierra_client.py if you run the scrapers from Streamlit Cloud.
# If you pre-build the DB locally and only deploy the dashboard (read-only mode),
# you can omit these.
SIERRA_BASE_URL           = "https://euronet.sierra.ai"
SIERRA_GRAPHQL_PATH       = "/-/api/graphql"
SIERRA_AGENT_ID           = "bot-01K5WBQ9Q5VK3FGTV2VVDT5T4C"
SIERRA_WORKSPACE_VERSION_ID = "workspaceversion-01KPV0JND8QVSW9H7ZQMY4PR90"
SIERRA_COOKIE             = "paste-cookie-header-string-here"
SIERRA_CSRF_TOKEN         = "paste-csrf-token-here"

# Needed if you re-run classification from Streamlit Cloud.
ANTHROPIC_API_KEY         = "sk-ant-..."
