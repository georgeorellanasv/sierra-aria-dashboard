# Sierra — cookies from the browser (DevTools > Application > Cookies > euronet.sierra.ai)
# Paste all cookies as "name1=value1; name2=value2; ..."
SIERRA_COOKIE=""

# The Aria agent ID (from the URL /agents/<ID>/sessions/)
SIERRA_AGENT_ID="01K5WBQ9Q5VK3FGTV2VVDT5T4C"

# Sierra base URL
SIERRA_BASE_URL="https://euronet.sierra.ai"

# Anthropic API key for Sonnet classification
ANTHROPIC_API_KEY=""

# Date range for the sessions scraper (YYYY-MM-DD). Defaults to today if empty.
SCRAPE_DATE_FROM=""
SCRAPE_DATE_TO=""
